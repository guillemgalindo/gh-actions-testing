import os, sys
from pprint import pprint
import re

test_string = (
    "This is a very long string with the wrong name format to see what black will do. It is also using single quotes"
    " instead of double quotes."
)
